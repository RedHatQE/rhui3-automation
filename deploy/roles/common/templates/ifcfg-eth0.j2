DEVICE="{{ ansible_eth0.device }}"
BOOTPROTO="dhcp"
ONBOOT="yes"
TYPE="Ethernet"
USERCTL="yes"
PEERDNS="no"
IPV6INIT="no"
{# link to the dns server ip address #}
DNS1="{{ hostvars[groups['DNS'][0]]['ansible_eth0']['ipv4']['address'] }}"
SEARCH="example.com"
